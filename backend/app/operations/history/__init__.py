"""Operations' official history — the complete trail of what happened to
one Operation (created -> started -> finished/failed), alongside the
already-existing current state. It knows nothing outside app.operations:
no Runtime, no Application, no CRM, no Workflow, no Presentation.
"""
