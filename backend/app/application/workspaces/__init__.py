"""Read-only workspace layers: aggregate already-persisted data across domains into
display-ready DTOs. No business rules, no recalculation — a future Frontend/API
consumer depends on these shapes instead of the domains directly.
"""
