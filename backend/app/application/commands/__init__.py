"""The platform's official registry of public commands — pure description,
no execution, no handler, no domain integration. Derived exclusively from
ApplicationCatalogService's own catalog; prepares the future Command Bus.
"""
