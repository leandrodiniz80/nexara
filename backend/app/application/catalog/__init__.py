"""The platform's official catalog of public operations — how any future
consumer (API, CLI, SDK, Dashboard, Workers, Scheduler, GraphQL) discovers
what the platform offers, without executing or integrating with anything.
Derived exclusively from PublicUseCaseService's own catalog; it knows
nothing about the internal domain.
"""
